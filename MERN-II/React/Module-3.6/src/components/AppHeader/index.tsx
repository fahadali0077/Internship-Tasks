/**
 * AppHeader — NOTE for Module 3.6
 *
 * This component is NOT used in the Router-based layout.
 * RootLayout (src/components/Layout/RootLayout.tsx) contains the full
 * navbar inline, using NavLink for active-route highlighting.
 *
 * AppHeader is kept here as a reference for the pre-Router App (Module 3).
 * It will be removed in Module 4 (Next.js) where the layout becomes a
 * Server Component in app/layout.tsx.
 *
 * If you need to add a shared header element, edit RootLayout directly.
 */

export {}; // prevent "no exports" TS error on an intentionally empty module
